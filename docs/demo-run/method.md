1. Read in full before running: prompts/core-map.md; prompts/skills/interview.md, capture.md, event-logger.md, questions.md; he/voice-canon.md; he/report-templates.md — the map governed every conflict.
2. Real, not imagined: every «העוזרת» turn was generated live as the suite's assistant against Noa's scripted messages (fresh install, direct-write path); the five files under records/ are the run's actual writes — one batched announce moment, no research unit, no tier letters or labels surfaced, canon opening and file headers verbatim.
3. Flow vs script: the form option folded into the first ask-me turn; who-for asked first (msg 3 fit as scripted); shape inferred from msg 4 so the still-looking/managing fork never fired; intent, "anything soon", clinician-access and guidance-level slots stayed unasked (One Small Move plus the scripted arc left them no turn — deferred, absent from profile); the allergy question fired at the first-medication moment (before msg 7); the doctor's "stress" assessment lives in the profile situation line and ש-02's reasoning, not as an undated timeline entry.
4. The mandated privacy-at-install line (naming the model-training opt-out) was placed at the save moment as the session's single close offer, which «לא הפעם, תודה» declines (its one repeat at first export remains due); the question ledger was seeded from the record's two gaps under Questions-Are-the-Action, declared in the reflect-back and confirmed by msg 8's «כן».
5. Uncertain, left honest: "מתחילת אפריל" vs "כבר חמישה שבועות" is internally inconsistent against 02.08.2026 — both kept verbatim with a gentle in-run note, never resolved by invention; Noa's grammatical gender never surfaces in her messages, so all address forms are written-form neutral; her display name was never captured (no scripted slot), so the profile field stays blank.
6. Correction pass (same date, post-transcript): core-map.md §4 now pins allergies as ONE field line beginning exactly «אלרגיות:» (never a section heading, for cross-tier parser compatibility) — verified in the map itself, then medications.md was re-emitted with «אלרגיות: אין ידועות (נמסר בשיחה, 02.08.2026)» replacing the former «## אלרגיות» section (value unchanged in meaning; no prior differing value, so no «אלרגיות (קודם):» line); the tag became «התאמה» (the map's one-word rule rules out the two-word «התאמת סכימה»); no conversational content or other file was touched, so the frozen transcript's «הקמה» verification line correctly reflects the pre-update session.
